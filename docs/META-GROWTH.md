# 局外成長・草案（v2）

> 狀態：**草案，還沒實作**。
> 依賴 [`BALANCE.md`](BALANCE.md) §7 的**難度層級**（動態難度已取消）。
> 數字取自 2026-09-11 雲端 `mg_scores` 的真實紀錄 ＋ `tools/tier-sim.mjs` 模擬。
> 相關：[`BALANCE.md`](BALANCE.md)（局內數值）、[`DATA.md`](DATA.md)（雲端欄位）。

---

## 0. 一頁講完

每打完一場，換成**成長點數**：

```
成長點數 = 6 ＋ (擊倒數 × 2 ＋ 到達等級) × 層級係數 ＋ floor(wpm / 5)
層級係數：初階 0.8　中階 1.4　高階 2.0
單場上限 = 30 × 場次分鐘數
```

點數拿去點三棵**天賦樹**（法杖／弓箭／匕首各一棵）。樹上有**小節點**（數值）
和**大節點**（機制）。**第 n 個點亮的節點花 `10 × n` 點**（大節點兩倍）。

5 分鐘場、中位學生一場 47 點：**第一場點得到兩個節點**，第三個約 1.3 場、
第五個約 3.2 場。最慢的孩子一場 15 點，第一場也點得到一個。

**這一版跟前一版最大的差別**：動態難度取消了，所以天賦的效果**不會再被吃掉**。
前一版整份文件都在處理「DDA 會把變強吃掉」這件事，現在那個問題從根上消失了。

---

## 1. 為什麼這次改動讓局外成長變得可行

前一版草案（有 DDA 的時候）最大的難題是：

```
點了天賦「傷害 +10%」→ 怪死得快 → 這場擊倒變多
                    → 下一場 DDA 把怪血調厚 → 擊倒數回到 4～8 隻
```

**變強永遠不會反映在畫面上。** 那時的解法是「把獎勵改成用『打得動多深』來呈現」，
繞了一大圈。現在難度曲線固定了（[`BALANCE.md`](BALANCE.md) §7），天賦直接有效：

| 傷害加成 | 中階 wpm 10 的 10 分鐘場 |
|---|---|
| +0% | 17.5 隻、到 LV12 |
| +15% | 20.0 隻、到 LV13 |
| +30% | 22.0 隻、到 LV14 |

**+30% 傷害 = 多打倒 20%、多爬 2 級。** 學生看得到自己變強了，這才有「局外成長」。

---

## 2. 公平怎麼辦：DDA 的工作換「層級」接手

DDA 除了調難度，還順手做了一件事：**讓快的孩子跟慢的孩子擊倒數差不多**，
所以擊倒數可以當「公平的貨幣」。拿掉 DDA 之後，這件事要有人接手。

接手的是**三個層級各自的難度曲線**。實測（`tools/tier-sim.mjs`，5 分鐘場）：

| 層級 | 學生 | 擊倒 | 到達 LV |
|---|---|---|---|
| 初階 | wpm 4 | 8.0 | 6.0 |
| 中階 | wpm 10 | 9.8 | 8.0 |
| 高階 | wpm 20 | 11.0 | 11.0 |

wpm 從 4 到 20 差了 5 倍，擊倒數只差 1.4 倍——**層級曲線本身就把擊倒數拉到同一個帶**。
所以擊倒數還是可以當貨幣，只是這次是「曲線」在做這件事，不是「每場偷偷調血量」。

**剩下的差距靠 `floor(wpm/5)` 這一項刻意拉開**，因為使用者要求
「速度快的同學一場的收益比較高」。

---

## 3. 成長點數

### 3.1 公式

```
成長點數 = 6 ＋ (擊倒數 × 2 ＋ 到達等級) × 層級係數 ＋ floor(wpm / 5)
```

| 部分 | 用意 |
|---|---|
| `6` | **保底**。打完一場一定有進展——見 §3.2 |
| `擊倒數 × 2` | 主幹。層級曲線已經把它拉到同一個帶（§2），所以它衡量的是「這場認真了沒」 |
| `到達等級` | 使用者說的「經驗值」。用等級而不是原始經驗，因為孩子看得懂（「我到 LV8」），而且已經存在雲端 |
| `× 層級係數` | 層級越高怪越硬、擊倒越少，係數補回來。**升層不能變虧**（§3.3） |
| `floor(wpm / 5)` | **不乘層級係數**，避免快的孩子被算兩次。這是刻意留給手速的差距 |

層級係數：**初階 0.8 / 中階 1.4 / 高階 2.0**（等距，好記）。

### 3.2 那個「+6」是保底，不是湊數

真實資料裡最弱的那個孩子（wpm 1、0 隻、LV1）不加保底一場只拿 1 點，
第一個節點要打 10 場才點得到——那他就不會再打了。
加了保底，他一場 7 點，兩場點到第一個。

### 3.3 升層不能變虧

升層之後怪變硬、擊倒數一定下降。驗算（2 分鐘場）：

| 學生 | 升層前 | 升層後 | 點數變化 |
|---|---|---|---|
| wpm 6（初階頂 → 中階） | 5.0 隻 / 18 點 | 3.0 隻 / 21 點 | **+17%** |
| wpm 13（中階頂 → 高階） | 5.0 隻 / 29 點 | 4.0 隻 / 35 點 | **+21%** |

**升層是獎勵。** 改層級係數的時候要重跑 `node tools/tier-sim.mjs` 的第 3 張表，
只要出現負的就是壞了。

### 3.4 各層級實際拿多少（5 分鐘場）

| 學生 | 層級 | 擊倒 | 到達 LV | **點數** |
|---|---|---|---|---|
| wpm 2 | 初階 | 4.0 | 3.0 | **15** |
| wpm 4 | 初階 | 8.0 | 6.0 | **24** |
| wpm 6 | 初階 | 11.0 | 7.0 | **30** |
| wpm 7 | 中階 | 7.0 | 7.0 | **36** |
| **wpm 10（中位）** | 中階 | 9.8 | 8.0 | **47** |
| wpm 13 | 中階 | 12.5 | 9.9 | **57** |
| wpm 14 | 高階 | 9.0 | 9.4 | **63** |
| wpm 20 | 高階 | 11.0 | 11.0 | **76** |
| wpm 30 | 高階 | 16.0 | 14.0 | **104** |

最快 / 最慢 = 6.9 倍。這比 DDA 版本（2.1 倍）大很多——**這是取消 DDA 的代價**，
見 §3.5。

### 3.5 ⚠ 快慢差距變大了，而且會跟著場次長度放大

| 場次 | 中位點數 | 最快 / 最慢 |
|---|---|---|
| 2 分鐘 | 26 | 5.5× |
| **5 分鐘** | **47** | **6.9×** |
| 10 分鐘 | 74 | 7.0× |

DDA 以前會壓住這個差距，現在壓不住了。能用的手段只有三個：

1. **層級係數**（§3.1）——已經在做，但它只補償「升層那一步」，不補償層級內的差距。
2. **單場上限 30 點／分鐘**——2 分鐘場 60 點、5 分鐘場 150 點。
   目前只有 wpm 30 的孩子在 2 分鐘場會撞到（60 點）。
3. **節點花費是 10n 遞增**——這是最有效的自動減速器：
   點數多 2 倍，點亮的節點數只多 √2 ≈ 1.4 倍。見 §4.3。

**這件事要盯**：上線兩週後用班級數據圖看真實的點數分布，
如果前段跟後段差到「後段永遠追不上」，就把 `floor(wpm/5)` 降成 `floor(wpm/8)`。

### 3.6 場次長度與防刷

- **中途逃脫不給點數**（`escapeBattle()` 本來就不寫成績），不能「打三題就跑」。
- **點數會隨場次長度增加**，這是對的：練越久拿越多。
  代價是「在家練很多的孩子」會拉開——§8 的每日上限是處理這個的旋鈕。
- 正確率不用另外算：真實資料顯示正確率中位是 100%（孩子是慢慢打打對，不是亂敲），
  而且 `wpm` 只算打對的字、怪也要打對才會掉血，亂按三項全低。

---

## 4. 成長曲線：第 n 個節點花 10n 點

### 4.1 花費規則

```
花費 = 10 × (已點亮的節點數 + 1)      小節點
     = 20 × (已點亮的節點數 + 1)      大節點
```

不看在哪一棵樹、不看第幾圈，**只看已經點亮幾個**。一句話講得完。

### 4.2 幾場點得到第 n 個（5 分鐘場）

| 第 n 個 | 花費 | 累計 | 最慢(15/場) | **中位(47)** | 最快(104) |
|---|---|---|---|---|---|
| 1 | 10 | 10 | **0.7 場** | **0.2** | 0.1 |
| 2 | 20 | 30 | 2.0 | **0.6** | 0.3 |
| 3 | 30 | 60 | 4.0 | **1.3** | 0.6 |
| 4 | 40 | 100 | 6.7 | **2.1** | 1.0 |
| 5 | 50 | 150 | 10.0 | **3.2** | 1.4 |
| 6 | 60 | 210 | 14.0 | **4.5** | 2.0 |
| 8 | 80 | 360 | 24.0 | **7.7** | 3.5 |
| 10 | 100 | 550 | 36.7 | **11.7** | 5.3 |
| 12 | 120 | 780 | 52.0 | **16.6** | 7.5 |

「第一場就點得到第一個」✓（連最慢的孩子都行）。
中位學生**第一場點得到 2 個**、第五個約 3 場。

> 使用者原本要的是「第一個 1 場、第二個 2 場、第三個 3 場」。
> 那是照 2 分鐘場算的。**改成 5 分鐘場之後前期會比較快**（第一場就兩個）。
> 想要回到原本的節奏，把花費從 `10n` 改成 `25n` 即可——
> 表格重算一次就好，曲線形狀不變。

### 4.3 一學期點得完嗎（20 堂課 × 2 場 = 40 場，5 分鐘場）

| 程度 | 一場 | 一學期 | 點亮節點數（全樹 21 個） |
|---|---|---|---|
| 最慢 | 15 | 600 | **10** |
| 中位 | 47 | 1880 | **18** |
| 最快 | 104 | 4160 | **28 → 點滿 21 個** |

**最快的孩子一學期會點滿。** 這是 `10n` 遞增擋不住的部分（他的點數是中位的 2.2 倍，
但只要 1.5 倍的節點數就點滿）。兩個選項：

- **接受**：他點滿了就沒東西可點，等於自然停在天花板，其他人繼續追。
- **加深**：節點從 21 個加到 30 個，或大節點再貴一點（3 倍價）。

建議先做 21 個上線看實際情況，不要一開始就把樹做太大。

### 4.4 混大節點的話（中位學生、5 分鐘場）

| 順序 | 花費 | 累計 | 要幾場 |
|---|---|---|---|
| 1 小 | 10 | 10 | 0.2 |
| 2 小 | 20 | 30 | 0.6 |
| **3 大** | **60** | 90 | **1.9** |
| 4 小 | 40 | 130 | 2.8 |
| 5 小 | 50 | 180 | 3.8 |
| **6 大** | **120** | 300 | **6.4** |
| **8 大** | **160** | 530 | **11.3** |

---

## 5. 三棵天賦樹

三棵對應三把武器，**天賦只在使用那把武器時生效**。這樣才會有人願意換武器
（現在幾乎所有人只用一把），也讓「這學期走哪一條」變成一個選擇。

每棵樹三圈：**要先點亮同一棵樹外圈的任一節點，才能點內圈**。
所以開局最多鋪 3 個便宜的第一圈節點，之後就得專精。

括號是它動到的 `BALANCE` 參數。

### 5.1 法杖・咒學（標點、長句、爆發）

| 圈 | 類型 | 名稱 | 效果 |
|---|---|---|---|
| 1 | 小 | 咒文入門 | 詠唱成功倍率 ×3 → ×3.3（`chantMult`） |
| 1 | 小 | 標點熟練 | 每個正確標點傷害 +1（`staffPunctPer` 3 → 4） |
| 2 | 小 | 沉穩 | 一般題全對額外 +8% 傷害 |
| 2 | 小 | 咒力凝聚 | 詠唱每 5 題 → 每 4 題（`specialEvery`，只對法杖） |
| 2 | **大** | **【餘韻】** | 詠唱成功後，接下來 **2 題**傷害 +50% |
| 3 | 小 | 標點大師 | 標點傷害再 +2（`staffPunctPer` → 6） |
| 3 | **大** | **【不中斷的詠唱】** | 詠唱題**錯一個字仍算成功**（倍率降為 ×2）—— 給常在最後一個字翻車的孩子 |

### 5.2 弓箭・專注（正確率、爆擊條、神射手）

| 圈 | 類型 | 名稱 | 效果 |
|---|---|---|---|
| 1 | 小 | 穩定呼吸 | 正確率加成 1.2 → 1.4（`atk.bowAcc`） |
| 1 | 小 | 快速蓄力 | 爆擊條每題 +25 → +30（四題集滿變三題半） |
| 2 | 小 | 神射延長 | 神射手 3 題 → 4 題（`sharpShots`） |
| 2 | 小 | 銳利箭頭 | 神射手倍率 ×2 → ×2.2（`sharpMult`） |
| 2 | **大** | **【蓄能上場】** | 每場**開場爆擊條就有 40%**，第二題就能進神射手 |
| 3 | 小 | 心無旁騖 | 該場正確率 ≥95% 時，全程額外 +10% 傷害 |
| 3 | **大** | **【連射】** | 神射手期間每題多射一箭（該題再 +35%），動畫兩支箭 |

### 5.3 匕首・迅捷（速度、連擊、連環斬）

| 圈 | 類型 | 名稱 | 效果 |
|---|---|---|---|
| 1 | 小 | 快手 | 連環斬速度斜率 +0.4（`flurry.slope`） |
| 1 | 小 | 連擊熟練 | 連擊上限 +90% → +105%（`combo.cap`） |
| 2 | 小 | 長刃 | 連環斬最長 12 → 15 字（`flurry.maxChars`） |
| 2 | 小 | 連擊加速 | 每連擊 +6% → +7%（`combo.step`） |
| 2 | **大** | **【追擊】** | 擊倒後**立刻對下一隻補一次同傷害的攻擊** |
| 3 | 小 | 不失手 | 連環斬最低倍率 0.9 → 1.2（打慢也不吃虧） |
| 3 | **大** | **【殘影】** | 連續 3 題全對後，下一題傷害**翻倍** |

### 5.4 ⚠ 沒有 DDA 之後，會加擊倒數的天賦要盯緊

DDA 以前會把「天賦讓我多殺幾隻」這件事在下一場吃掉，所以怎麼設計都不會失控。
**現在沒有那道保險了**：

```
【追擊】→ 擊倒數 +1~2 → 成長點數變多 → 更快點到下一個天賦 → 擊倒數再 +
```

這是一個**正回饋**。目前的減速器是 `10n` 遞增花費與單場上限，應該夠，
但上線後要特別看【追擊】這種節點有沒有讓少數人暴衝。
真的失控就把點數的擊倒項改成「怪物等級加權」（打倒 LV10 的怪比 LV1 值錢），
而不是單純數隻數。

---

## 6. 跟全班比賽的關係

| 東西 | 會被天賦影響 | 結論 |
|---|---|---|
| **階級徽章／個人排名**（`bestScore` 戰績分＝每分鐘正確字數 × 品質 × 正確率） | **不會** | 三個因子天賦都不碰，排名天生公平 ✓ |
| **班內排行（看 `damage`）** | **會** | 要處理 |
| **全班總傷害推王** | 會 | 合作目標，大家一起變強是好事 ✓ |

**建議**：班內排行也改看**戰績分**（跟徽章同一把尺），`damage` 留給「全班總傷害」。

比賽場要不要**統一層級**也要決定（例如全班都用中階打比賽）——
不然不同層級的學生打的是不同難度的怪，`damage` 更沒有可比性。
但戰績分不受層級影響，所以只要排行改看戰績分，這件事就不急。

---

## 7. 雲端資料

接 [`DATA.md`](DATA.md) §3，`mg_scores` 一人一筆，加欄位就好：

| 欄位 | 型別 | 說明 |
|---|---|---|
| `tier` | string | `'A'` / `'B'` / `'C'`（難度層級，BALANCE.md §7） |
| `tierSetBy` | string | `'tutorial'` / `'promote'` / `'teacher'`，出問題時查得到是誰改的 |
| `growthPts` | number | 目前**可用**點數 |
| `growthTotal` | number | 史上累積拿過的總點數（不會被花掉，給老師看努力程度） |
| `talents` | map | `{ staff_punct:1, bow_gauge:1 }` |
| `talentCount` | number | 已點亮幾個（＝下一個的價錢） |
| `bestZone` | number | 史上打到最深的區域 |
| ~~`dda`~~ | number | **不再讀**，保留不刪（回頭比對時查得到） |

**Firestore 規則不用改**（`mg_scores` 是欄位無關的），見 [`DATA.md`](DATA.md) §2。

三個已知的坑（細節在 `DATA.md` §3）：

1. **身分綁在「班級_座號」**：換班、老師改座號表，天賦跟層級都會不見。
2. **匿名登入擋不住改數字**：開主控台就能把 `growthPts` 改成 9999，
   或把 `tier` 改成 `'A'` 來打軟怪刷點。
   最低成本的緩解：一場最多 `30 × 分鐘` 點，`growthTotal > plays × 上限`
   就一定有問題，教師端標紅；`tier` 跟 `bestWpm` 差太多也標紅。
3. **累積量沒在存**：`bestKills` 是「單場最多」不是「總共」。

---

## 8. 介面

- **結算畫面**：新增一行 `成長點數 +47`，按一下展開拆解
  （`保底 6｜(擊倒 9.8×2 + LV8) × 中階 1.4 = 39｜速度 10 wpm = 2`）。
- **營地**：新增「天賦」按鈕，旁邊顯示可用點數（有點數沒花時加小紅點）。
  層級也顯示在這裡（「你目前是 中階」），達到升層條件時出現挑戰按鈕（§7.4）。
- **天賦面板**：三棵樹並排，點亮的發光、可點的金框、買不起的灰掉並顯示還差幾點。
- **戰鬥中不顯示天賦**（畫面已經很擠）。
- **洗點**：營地一顆「重置天賦」，**免費、隨時可以**。

---

## 9. 這份草案的資料極限

- 點數公式的係數是拿 **27 筆真實 v2 紀錄**校準的，但那 27 筆是**有 DDA 時**打的。
  換成層級曲線之後實際分布一定會變。
- `tools/tier-sim.mjs` 的模擬**沒有連擊、沒有特殊攻擊**，數字會偏低 10～20%。
- **層級門檻（6 / 14）是從 187 筆 `bestWpm` 推的**，但教學量到的 wpm 比 `bestWpm` 低，
  實際分層會偏向初階。上線後第一件事就是看三層各分到幾個人。

**流程**：

1. 先做 **B1～B3**（層級系統本身，還沒有天賦），觀察兩週。
2. 用班級數據圖看三層人數比例、各層的 `lastKills` 分布。
3. 門檻與曲線調到「三層人數大致 1:2:1、各層擊倒數都落在 8～12 隻（5 分鐘場）」。
4. 再做 M1～M3（點數只顯示不能花），又兩週。
5. 最後才做 M4～M7 的天賦樹。

---

## 10. 施工項目

### 第一階段：難度層級（取代 DDA）

| 編號 | 項目 | 依賴 |
|---|---|---|
| B1 | `BALANCE.tier`：三層的 `ctk0` / `slope` / `expMult` / `ptK` / 門檻；移除 `BALANCE.dda` | — |
| B2 | `ctkAt(lv, tier)` 改線性、`monsterHp()` 拿掉 `dda` 參數；移除 `seedDda()` / `updateDda()` / rescue | B1 |
| B3 | 新手教學結束寫 `tier`；舊學生用 `bestWpm` 補寫；跳過教學給中階 | B2 |
| B4 | 營地顯示目前層級；達標時出現「挑戰下一層」按鈕（§7.4 的遲滯與確認） | B3 |
| B5 | 教師控制台：看／改每個學生的層級（接在班級數據圖旁邊） | B3 |
| B6 | `tools/tier-sim.mjs` 改成讀 `BALANCE.tier`（現在是寫死規格值） | B1 |

### 第二階段：成長點數（先只顯示）

| 編號 | 項目 | 依賴 |
|---|---|---|
| M1 | `BALANCE.growth`：係數、保底、每分鐘上限、節點花費公式 | B1 |
| M2 | `growthPointsOf(G)`，寫進 `saveScore()` | M1 |
| M3 | 結算畫面「成長點數 +N」與拆解 | M2 |

### 第三階段：天賦樹

| 編號 | 項目 | 依賴 |
|---|---|---|
| M4 | `TALENTS` 資料表：21 個節點 | M1 |
| M5 | `applyTalents()`：疊到 `BALANCE` 的**副本**上（不要改到原本的） | M4 |
| M6 | 天賦面板 UI | M4 |
| M7 | 七個大節點機制 | M5 |
| M8 | `bestZone` 與結算的「最深區域」呈現 | — |
| M9 | 班內排行改看戰績分（§6） | — |
| M10 | 教師端：天賦總覽、重置全班天賦、異常標紅 | M2 |

---

## 11. 還沒決定，要老師拍板

1. **場次長度**：模擬顯示 **5 分鐘**最合適（2 分鐘看不出天賦效果，10 分鐘差距太大）。
   現在雲端設定是 120 秒，要改嗎？
2. **比賽場要不要統一層級**（§6）。
3. **學期交接**：層級與天賦跨學期保留還是重置？
   （層級建議保留——那是學生真實能力的紀錄；天賦可以重置。）
4. **在家練習算不算**：目前紀錄**全部**來自練習。要不要設每日上限？
5. **最快的孩子一學期會點滿 21 個**（§4.3），接受還是把樹做深？
